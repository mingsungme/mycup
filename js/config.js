/* ============================================================
   My Cup — API 설정 파일
   ------------------------------------------------------------
   ⚠️ 보안: 이 파일에 키를 넣은 채로 공개 저장소에 커밋하지 마세요.
   키는 앱 실행 후 ⚙ 설정 화면에서 입력하는 것을 권장합니다.
   (설정 화면 입력값은 이 브라우저의 localStorage에만 저장됩니다)

   YouTube Data API v3 키 발급 방법:
   1. GCP Console (console.cloud.google.com) → 새 프로젝트 생성
      (예: mycup-musicappdesign)
   2. "API 및 서비스" → 라이브러리 → "YouTube Data API v3" 검색 → 사용 설정
   3. "사용자 인증 정보" → API 키 만들기
   4. 발급된 키를 앱 ⚙ 설정에 붙여넣기 (또는 아래에 입력)

   iTunes Search API는 키가 필요 없습니다 (30초 미리듣기 무료).
   YouTube 키가 없으면 큐레이션된 영상으로 '데모 모드' 동작합니다.
   ============================================================ */
window.MYCUP_CONFIG = {
  YOUTUBE_API_KEY: '' // 비워두면 ⚙ 설정의 키 → 없으면 데모 모드
};
